# Future Plans
In here i will talk about potential future features to implement into this ORM.

### Ideas
- ✅ strict types -> models will need to define a structure with properties.
- ❌ CLI tool for migrations -> upon running a migration a corrisponding model class will be created in the name of the table with the corresponding columns predefined as class properties (ofcourse this option will be optional maybe through including a flag of sorts).